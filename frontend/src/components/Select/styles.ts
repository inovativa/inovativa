import styled from 'styled-components';

const SelecStyles = styled.select`
  width: 100%;
  border-radius: 0.6rem;
  background: transparent;
  padding: 0.5rem;

  font: Signika;

  color: #808080;
  font-size: 1.6rem;
`;
export default SelecStyles;
